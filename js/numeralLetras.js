// function numeralConLetras(event) {
//     const numeral = event.target;
//     numeral.value = numeral.value.replace(/\s/g, ''); // Eliminar espacios en blanco
  
//     // Verificar si el símbolo "#" ya está presente al inicio del valor
//     const symbolExists = numeral.value.startsWith('#');
  
//     if (!symbolExists && numeral.value.length > 0) {
//       numeral.value = '#' + numeral.value;
//     } else if (symbolExists && numeral.value.length == 0) {
//       numeral.value = '';
//     }
//   }
  